
query DataQEpisode($identifier: PodcastIdentifier!, $sort: EpisodeSort) {
    podcast(identifier: $identifier) {
        episodes(sort: $sort) {
            data {
                id
                title
                description
                imageUrl
            }
        }
    }
}